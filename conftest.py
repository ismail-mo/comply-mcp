# Root conftest — makes formulas/, tables/, registries/, engine/ importable
# when pytest runs from the repository root (pytest prepends this directory
# to sys.path because this conftest.py lives at rootdir).
